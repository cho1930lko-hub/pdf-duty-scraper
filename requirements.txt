# ─── Core ───────────────────────────────────────────────────
streamlit>=1.35.0
gspread>=6.0.0
google-auth>=2.28.0
pandas>=2.2.0
requests>=2.31.0
openpyxl>=3.1.0

# ─── PDF Support ────────────────────────────────────────────
pdfplumber>=0.11.0      # Primary PDF text extraction
PyMuPDF>=1.24.0         # Fallback PDF extraction (import as fitz)

# ─── Optional Image Support ─────────────────────────────────
Pillow>=10.3.0

# ─── AI Providers (no extra package needed) ─────────────────
# Groq   → requests (GROQ_API_KEY in secrets)
# DeepSeek → requests (DEEPSEEK_API_KEY in secrets)
# Gemini → requests (GEMINI_API_KEY in secrets)
